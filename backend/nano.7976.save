# 1. Chọn Node LTS
FROM node:18

# 2. Tạo thư mục làm việc trong container
WORKDIR /app

# 3. Copy package.json và package-lock.json
COPY package*.json ./

# 4. Cài thư viện
RUN npm install --production

# 5. Copy toàn bộ source code vào container
COPY . .

# 6. Expose port (thay bằng port API của bạn)
EXPOSE 3000

# 7. Chạy app
CMD ["npm", "start"]

